# 修改密码

```shell
# 修改当前登录用户的密码
passwd

# 修改指定用户的密码（需要root权限）
passwd username

# 无交互式修改密码
# echo "new_password" | passwd --stdin username
# 示例：修改root账号密码为centos
echo "centos" | passwd --stdin root
```

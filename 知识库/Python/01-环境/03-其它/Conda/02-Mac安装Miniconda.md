# Miniconda

Miniconda（轻量级，只包含 Python 和 Conda 核心组件）而不是几 GB 大小的全量版 Anaconda。

```shell
# 安装 Miniconda
brew install --cask miniconda
# 初始化 Conda
conda init zsh
source ~/.zshrc
# 验证
conda --version
# conda 25.3.1
```

